#include "deck.h"
#include <stdlib.h>
#include <stdio.h>
void sort_deck(deck_node_t **deck)
{
	card_t.kind_t;
	card_t.value;
	current->node = deck_node_t;
	for(enum card_t.kind_t= 0; card_t.kind_t <= sizeof(card_t.kind_t)/sizeof(card_t.kind_t[0]); card_t.kind_t ++)
		for(j = card_t.value; j <= 10 || K, J, Q; j++)
			printf("%s %d", card_t.kind_t[i] card_t.value[j]);
	current->next;
}
